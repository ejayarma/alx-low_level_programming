#include <stdio.h>
#include "main.h"

/**
 * main - Main entry for the program
 * Check the code to see what it does
 * Return: Returns 0 always
 */
int main(void)
{
	short i;

	for (i = 97; i < 123; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (0);
}
