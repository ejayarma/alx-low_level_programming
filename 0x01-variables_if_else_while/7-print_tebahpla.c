#include <stdio.h>

/**
 * main - Main entry for the program
 * Return: Returns 0
 */
int main(void)
{
	unsigned int i;

	for (i = 122; i > 96; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
